name: Build
on:
  - push
  - workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v6
      - run: make
      - uses: actions/upload-artifact@v7
        with:
          name: artifact
          path: target/*.pchtxt
